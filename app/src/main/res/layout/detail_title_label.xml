<?xml version="1.0" encoding="utf-8"?>
<TextView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tv_detail_title_label"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:textColor="@color/white"
    android:textSize="@dimen/text_size_detail_title"
    android:paddingHorizontal="@dimen/padding_horizontal_detail_title"
    android:paddingTop="@dimen/padding_top_detail_title"
    android:paddingBottom="@dimen/padding_bottom_detail_title"/>